# Unslop

Stops Claude, Codex, and Grok from writing like chatbots.

Local skill plus hooks. Injected before every reply and every subagent. Nothing fetches a URL.

```bash
git clone https://github.com/Vuk97/unslop
cd unslop
./install.sh
```

Restart Claude, Codex, and Grok. In Codex, run `/hooks` and trust `unslop.py`.

Apache-2.0. Style digest from the [Google developer documentation style guide](https://developers.google.com/style), CC BY 4.0.
